import { x } from '@xstyled/styled-components';

const PageFooter = (): JSX.Element =>
    (
        <x.footer p={12}>
            <hr />
            <p>Page Footer</p>
        </x.footer>
    );

export default PageFooter;
